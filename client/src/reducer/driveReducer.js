import { ADD_DRIVE, GET_DRIVES } from '../actions/types';

const initialstate = {
  drives: [],
};

export default (state = initialstate, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
